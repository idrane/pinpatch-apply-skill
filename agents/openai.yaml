interface:
  display_name: "Apply PinPatch Notes"
  short_description: "Apply PinPatch notes from live storage or exports"
  default_prompt: "Use $pinpatch-apply to process these PinPatch notes or exports, update the code, verify changes, and return any processed archives."
